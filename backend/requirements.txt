fastapi==0.139.0
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
aiosqlite==0.22.1
greenlet==3.5.3
pydantic==2.13.4
pydantic-settings==2.14.2
argon2-cffi==25.1.0
cryptography==49.0.0
itsdangerous==2.2.0
slowapi==0.1.10
apscheduler==3.11.3
httpx==0.28.1
python-multipart==0.0.32
# mcp held at 1.22.0: 1.23.0+ enables DNS-rebinding Host validation by default
# and answers HTTP 421 for any Host not in an explicit allowlist (breaks the
# /mcp tests and any reverse-proxied deployment). Upgrading past 1.22.0 needs
# TransportSecuritySettings(allowed_hosts=[...]) wired into build_mcp_app().
mcp==1.22.0
aiomqtt==2.5.1
