fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
aiosqlite==0.20.0
greenlet==3.1.1
pydantic==2.9.2
pydantic-settings==2.5.2
argon2-cffi==23.1.0
cryptography==43.0.1
itsdangerous==2.2.0
slowapi==0.1.9
sse-starlette==2.1.3
apscheduler==3.10.4
httpx==0.27.2
python-multipart==0.0.10
# pycupra: bumped to v0.2.33 (2026-06-07) — resets loginInProgress when
# authorize() raises, so failed logins can be retried instead of wedging.
# KNOWN OUTAGE: since 2026-06-08 ~12:00 CEST VW/CARIAD block third-party API
# access server-side (Akamai 403 on /garage/vehicles even with valid tokens,
# likely app attestation). NO pycupra version fixes this — official app working
# no longer implies a pin bump will help. Track HA-pycupra #99; read-only
# fallback is the EU Data Act portal (WulfgarW/ha_get_euda_data, 15-min lag).
pycupra @ git+https://github.com/WulfgarW/pycupra.git@v0.2.33
